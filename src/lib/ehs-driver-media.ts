/** Media attachments for EHS driver license scans (entity_id = ehs_approved_drivers.id). */
export const EHS_DRIVER_MEDIA_ENTITY = "ehs_driver_record";

/**
 * Media attachments for per-authorization training records
 * (entity_id = ehs_operator_authorizations.id).
 */
export const EHS_OPERATOR_AUTH_MEDIA_ENTITY = "ehs_operator_auth";
